---
title: "Scheduling"
description: "How Maigrate schedules interviews — from initial outreach through booking, reminders, and no-shows."
---

Once a candidate is advanced, the scheduling agent takes over. It reads recruiter calendars, proposes interview slots, handles the candidate's reply, and books the Google Calendar event.

## How scheduling starts

Scheduling can be triggered two ways:

1. **Automatically** — when `AUTO_SCHEDULE_ON_ADVANCE` is enabled, advancing a candidate (by the agent or by recruiter override) queues scheduling immediately
2. **Manually** — click **Schedule Interview** in the Details tab of the application drawer

When triggered, the scheduling agent:
1. Assigns a recruiter if one isn't already assigned
2. Reads the recruiter's Google Calendar for the next 7 days
3. Generates up to 3 available interview slots (30-minute blocks within business hours, skipping days already at their `max_interviews_per_day` cap)
4. Creates a communication thread and queues the communication agent to send the first outreach

## Timing rules

The scheduling agent operates inside fixed windows, all evaluated in the candidate's local timezone:

| Rule | Default |
|---|---|
| SMS delivery window | 8 AM – 9 PM candidate-local |
| Follow-up send window | 8 AM – 7 PM candidate-local |
| Follow-ups on weekends | Disabled unless configured |
| Initial slot search window | Next 7 days |
| Slot count and duration | Up to 3 slots, 30 minutes each |
| Weekend / evening exception requests | Always escalated to a recruiter |

If a candidate asks for a slot outside business hours, the agent will not unilaterally accept or decline — it escalates and waits for [operator guidance](/recruiter/communication#operator-guidance). See [Follow-up policy](/recruiter/follow-up-policy) for the full cadence.

## The initial outreach

The first message to the candidate proposes the available slots (or a booking link, if one is configured). The message is sent via SMS if the candidate has a valid mobile number, or email if not.

After the outreach is sent, the application status moves to `Awaiting candidate response`.

## What happens when the candidate replies

The communication agent handles inbound replies:

| Reply type | Agent action |
|---|---|
| Selects a slot | Books the interview on Google Calendar, sends a confirmation, status → `Scheduled` |
| Asks for different times | Searches for new availability and responds with options |
| Asks a question | Answers using the knowledge base, stays in scheduling mode |
| Weekend/after-hours request | Escalates to a human (policy: don't force daytime alternatives unilaterally) |
| Ambiguous or concerning reply | Escalates to a human |
| No reply | Follow-up automation kicks in (see below) |

## Follow-up automation

If a candidate doesn't respond, Maigrate follows up automatically based on the follow-up policy configured in [Settings → Scheduling](/settings/scheduling) and [Settings → Communication](/settings/communication). The timing, number of attempts, and which channel each follow-up uses are all configurable.

All sends respect a quiet-hours window in the candidate's local timezone. After the maximum attempts are exhausted, the thread is marked `unresponsive` and status becomes `Failed outreach`.

## Booking and confirmation

The scheduling agent books the interview only after the candidate confirms a slot. When booking happens, the agent:
- Creates or updates a Google Calendar event on the recruiter's calendar
- Requests a Google Meet link automatically
- Sends Google Calendar invites to the recruiter and the candidate
- Records the interview in Maigrate
- Replies to the candidate in the thread with a confirmation message using the **Interview Confirmation** template

The confirmation message is sent as part of the same agent turn that persists the booking — it isn't a separately scheduled follow-up. Wording is configurable in [Settings → Communication](/settings/communication) under "Interview Confirmations".

<Note>
  **Don't fight the calendar.** Google Calendar is the booking source of truth. If a candidate claims they already booked or interviewed, the agent reconciles by exact attendee-email match. Manual edits that contradict the calendar will lose to the calendar.
</Note>

## Interview reminders

Maigrate sends a single SMS reminder on the day of a confirmed interview:

- **Target send time**: ~8 AM in the candidate's local timezone, on the interview day
- **Channel**: SMS (via the configured Twilio number)
- **Template**: the **Same-Day Reminder** template from [Settings → Communication](/settings/communication)

The reminder is queued the morning of the interview and delivered as soon as the hourly send worker fires after 8 AM local. Two cases skip the reminder:

| Case | What happens |
|---|---|
| Interview is at or before 8 AM candidate-local | Skipped — no time to fit the reminder |
| Delivery slips past 7 PM local or past the interview start time | Window missed — no reminder sent |

The 8 AM start and 7 PM cutoff are the global follow-up send-window bounds, configurable via `FOLLOW_UP_START_HOUR_LOCAL` and `FOLLOW_UP_END_HOUR_LOCAL`.

## Recording the interview outcome

After every scheduled interview, the recruiter needs to record what happened. This is a required step — without it, the application sits in a stale state and downstream automation can't tell whether to follow up, reschedule, or move on.

<Frame>
  <img src="/images/interview-resolution.png" alt="Application drawer showing the Interview Completed and No-Show action cards that appear after a scheduled interview time has passed" />
</Frame>

Once the scheduled interview time has passed, two action cards appear on the **Details** tab of the application drawer:

- **Interview Completed** — click this when the interview happened. The application drops out of the default board view.
- **No-Show** — click this if the candidate didn't attend. You can add an optional private note for the scheduling agent. The scheduling agent then reaches out to reschedule.

### Standard operating procedure

For every interview on your calendar, do **one** of the following the same day:

1. **In Maigrate AI Hub** (recommended) — open the application drawer and click **Interview Completed** or **No-Show**. This is the cleanest path and gives the agent the most context.
2. **In your ATS** — if you mark the outcome on the contact record in your ATS instead, that's fine — the data flows back to Maigrate and the application state is updated.

Either path works. The important thing is that the outcome is recorded somewhere, every time. Letting interviews sit unresolved is the single most common cause of stalled pipelines.

### What happens next

**Interview Completed**: closes the scheduling workflow. The application is considered handed off to the rest of your hiring process.

**No-Show**: triggers the scheduling agent to reach out and offer the candidate a new slot.

**Repeat no-show**: a second no-show creates an escalation in the Command Center with a reject recommendation. You decide whether to reject or give the candidate another chance.

## Scheduling settings

You can configure slot duration, recruiter business hours, follow-up timing, and scheduling guidance in [Settings → Scheduling](/settings/scheduling).
